# Branching & Pull Request Workflow

1. Never commit directly to `main`.
2. Create a feature branch for every new task: `git checkout -b feature/your-feature-name`.
3. Write clear, descriptive commit messages.
4. Push the feature branch to GitHub and open a Pull Request (PR) against `main`.
5. Merge the PR only after review/verification.